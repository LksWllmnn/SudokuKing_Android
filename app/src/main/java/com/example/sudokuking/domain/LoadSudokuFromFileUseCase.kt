package com.example.sudokuking.domain

import java.io.File

class LoadSudokuFromFileUseCase {
    /*fun loadSudokuFile(sudokuIndex: Int): Array<Byte> {
        val file = File("../../sudokuFiles/sudoku")
        val bufferedInputStream = file.bufferedReader().useLines {
            lines -> lines.forEachIndexed()
        }


    }*/
}